/// App-wide constants.
abstract final class AppConstants {
  /// GitHub repository used for the in-app update check
  /// (public repo `releases/latest`, no auth required).
  static const String githubOwner = 'Mohammed-Asfar';
  static const String githubRepo = 'veetubook_app';
}
